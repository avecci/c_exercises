#include <stdio.h>
/* Main palauttaa integerin, ottaa sisään intin ja charin. */
int main(int argc, char *argv[])
{
    int voltit = 32;
    int distance = 100;
    // Yksirivinen kommentti.
    printf("Leijonassa on %d %% alkoholia. \n", voltit);
    printf("Juoksin %d metriä. \n", distance);
    return 0;

}
